define([
  'jquery',
  'Underscore',
  'Backbone',
	'backbone/models/user_icon'
], function($, _, Backbone, UserIcon){
	
	// A collection of User icons. We don't want the overhead of actual User objects.
	//
	return Backbone.Collection.extend({

	})

});
